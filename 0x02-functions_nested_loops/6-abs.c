#include "main.h"

/**
 * _abs - absolute value
 * @n: computing the absolute value
 *
 * Return: 1 of 0
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
