#include "main.h"
#include <stdio.h>

/**
 * mul - Funtion that acccepts two arguments
 * @a: first argument
 * @b: Second argument
 * Return: multiplied value
 */

int mul(int a, int b);
{
	return (a * b);
}
