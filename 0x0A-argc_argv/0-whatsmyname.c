#include <stdio.h>

/**
 * main - functionto print its name
 * @argc: argument count
 * @argv:  array of arguments passed
 * Return: return 0
 */

int main(int argc __attribute ((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
